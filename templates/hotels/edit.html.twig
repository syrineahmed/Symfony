{% extends 'YassineTemp/back.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-body">

                {{ include('hotels/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_hotels_index') }}" class="btn btn-secondary mt-3">Back to List</a>

                {{ include('hotels/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
