{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>NumPassport</th>
                <td>{{ facture.numPassport }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ facture.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ facture.prenom }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ facture.destination }}</td>
            </tr>
            <tr>
                <th>NomHotel</th>
                <td>{{ facture.nomHotel }}</td>
            </tr>
            <tr>
                <th>NomCompagnie</th>
                <td>{{ facture.nomCompagnie }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ facture.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_facture_index') }}">back to list</a>

    <a href="{{ path('app_facture_edit', {'numPassport': facture.numPassport}) }}">edit</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
