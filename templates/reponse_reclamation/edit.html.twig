{% extends 'admin/base.html.twig' %}

{% block title %}Edit ReponseReclamation{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Edit ReponseReclamation</h1>

        <div class="card">
            <div class="card-body">
                {{ include('reponse_reclamation/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="mt-3">
                    <a href="{{ path('app_reponse_reclamation_index') }}" class="btn btn-primary">Back to List</a>
                </div>

                <div class="mt-3">
                    {{ include('reponse_reclamation/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
