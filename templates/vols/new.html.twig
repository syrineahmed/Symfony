{% extends 'YassineTemp/back.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Create New Vols</h1>

        <div class="card">
            <div class="card-body">
                {{ include('vols/_form.html.twig') }}
            </div>
        </div>

        <a href="{{ path('app_vols_index') }}" class="btn btn-secondary mt-3">Back to List</a>
    </div>
{% endblock %}
